language: generic
before_install:
  - composer install
  - npm install
jobs:
  include:
    - stage: "Tests"
      name: "Unit Tests"
      script: ./vendor/bin/phpunit
    - stage: "Build"
      name: "JS/CSS compilation"
      script: npm run production
cache:
  directories:
  - vendor
  - node_modules